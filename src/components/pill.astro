---
const { label } = Astro.props;
---

<div class={`${label.toLocaleLowerCase()} px-2 py-0.5 flex items-center gap-1`}>
  <img class="w-3 h-3 xl:w-5 xl:h-5 2xl:w-8 2xl:h-8" src={`../icons/${label.toLocaleLowerCase()}.svg`} alt="tag-icon" />
  <p class="text-xs md:text-md xl:text-lg 2xl:text-xl">{label}</p>
</div>
